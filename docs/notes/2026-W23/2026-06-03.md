---
date: 2026-06-03
project: minimonitor
type: session-note
week: 2026-W23
billable: —
tags: [project/minimonitor, session-note, week/2026-W23]
---

# minimonitor — 2026-06-03

## State
- Repo: `~/Desktop/1/tools/minimonitor`, branch `main` @ `88128ab`. Now a **Cargo workspace** (was a single crate).
- The **new lean build is installed and running as the login LaunchAgent** `com.caguabot.minimonitor` (≈48 MB RSS, 0.1% CPU). Binary at `~/Applications/MiniMonitor/bin/minimonitor` (1.5 MB). It survives reboot.
- Cold restart / run:
  - `cargo run -p minimonitor` — the macOS tray app
  - `cargo run -p minimonitor-agent` — headless server on `127.0.0.1:9909` (`/snapshot` JSON, `/healthz`)
  - `cargo run -p minimonitor-agent -- --once` — one JSON snapshot to stdout
  - Reinstall the LaunchAgent after changes: `./scripts/install.sh`
- Design docs: `docs/superpowers/specs/2026-06-03-minimonitor-core-agent-refactor-design.md`, the implementation plan `docs/superpowers/plans/2026-06-03-minimonitor-core-agent-refactor.md`, and the spike runbook `docs/superpowers/specs/2026-06-03-beszel-spike-runbook.md`.

## Did
- **Re-architected single crate → Cargo workspace** (16 commits, merged to `main` fast-forward): `crates/core` (cross-platform collection lib), `crates/agent` (headless), `crates/menubar` (macOS tray + wry inspector; links core).
- **Decluttered:** removed the Token Checker + Provider Keys cluster (`services.rs` deleted; dropped `keyring`, `keyring-core`, `reqwest`, `tiktoken-rs`). Binary **17 MB → 1.5 MB** (~11×).
- **Enriched `core`:** listening ports (port→process via `lsof`), established-connection counts, disk-volume capacity (sysinfo `Disks`), uptime/boot, network identity (host/LAN/tailnet), sustained-CPU EWMA ("energy" proxy). `MonitorSnapshot` is now serde-`Serialize`. lsof/disk/identity throttled to ~10s (not per-second).
- **Menubar:** condensed 3-line tray header; "Listening ports (N)" submenu with Kill-owner; "Quick actions" submenu (caffeinate keep-awake, flush-DNS via osascript admin); reorganized inspector (System strip, ports table, disk volumes, connections, per-core, Sust column + Energy sort). Token/provider panels gone.
- **Agent:** serves `GET /snapshot` (JSON) + `/healthz` on `127.0.0.1:9909`; `--once`; `push.rs` is a no-op `Sink`/`NoopSink` stub (hub push intentionally deferred).
- **Bugfix:** LAN-IP detection now uses the default-route interface (`route get default` → `ipconfig getifaddr`) instead of hardcoded `en0`; resolves the mini's active `en1` (`192.168.100.104`). Clean tray startup verified (the prior tray-crash class).
- **Quality:** 7 `core` unit tests (lsof parse ×5, EWMA ×2) green; clippy clean. Executed subagent-driven (15 tasks, each spec-compliance + code-quality reviewed); final whole-branch review (opus) = ready to merge, agent run live against real data.
- Installed the lean build over the old 17 MB one via `scripts/install.sh`.

## Decided
- **Re-architect, not rewrite** — keep the collection core (already serialization-ready) and split collector from presenter, over starting fresh.
- **Fleet transport = agentless probes (ping/TCP/HTTP for any IP, incl. external/3rd-party) + push-from-agent for owned boxes**, over pull-over-tailnet — because external/NAT'd boxes break pull.
- **Build-vs-buy: spike Beszel + Uptime-Kuma (1 hr) before building a bespoke hub** — they cover ~80%. The hub + agent→hub push protocol are deferred behind that spike (push.rs left a stub) so this refactor isn't wasted whichever way it goes.
- Kept AI-workload detection + `AI{n}` tray hint; cut only the token/provider cluster.
- DND quick action deferred (no stable public macOS Focus API); shipped caffeinate + flush-DNS only.
- Menubar links `core` directly (samples in-process), not an agent HTTP client — for now (`core` shaped so it can flip later).

## Next
- **Run the 1-hour Beszel + Uptime-Kuma spike** — `docs/superpowers/specs/2026-06-03-beszel-spike-runbook.md`. Record outcome `ADOPT` (drop the custom hub; minimonitor stays menu-bar + core) or `BUILD` (bespoke hub on this agent). This is the gate for the "view from far + add devices" capability.
- If `BUILD`: hub MVP (scrape/probe → SQLite → web dashboard over Tailscale), then agent→hub push (`ureq`), then Linux agent ports/GPU (`ss`/`nvidia-smi`) as a fast-follow.
- Optional: push `main` to GitHub (`origin` is behind at `ae36c0b`; the remote URL embeds a plaintext token — switch to a credential helper). Onboard minimonitor into the vault + Command Center (currently untracked — see below).
- Blocker: none.

## Wiki?
"Local tool → fleet-ready" recipe: split a single-process monitor into `core` lib + headless `agent` (serves serde JSON over localhost) + thin UI, keeping the collector and replacing the delivery tier; cross-platform via `sysinfo` + `#[cfg]`-gated OS specifics; defer the hub/transport behind an off-the-shelf-vs-build spike so the refactor is never wasted. Generalizable across the other single-host tools.

## Signals
next_action: "Run the 1hr Beszel + Uptime-Kuma spike (docs/superpowers/specs/2026-06-03-beszel-spike-runbook.md) to decide build-vs-buy for the minimonitor fleet hub"
milestone: shipped
