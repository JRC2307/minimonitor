---
date: 2026-08-05
project: minimonitor
type: session-note
week: 2026-W32
billable: —
tags: [project/minimonitor, session-note, week/2026-W32, auto-scribed]
---
# minimonitor — 2026-08-05

## State
caguastore now has a live, passkey-free home tickers widget and `$TICKER` quote lookup, deployed on caguaserver. Task 1354 is marked done; the old PIN-gated mercado screen remains separate and unchanged. To restart cold, work in `~/Desktop/1/tools/minimonitor`, run `cargo build -p fleet`, deploy with rsync, restart `fleet-serve` and `minimonitor-agent`, then verify `/api/tickers` and the home page.

## Did
- Added the unlocked `/api/tickers` endpoint and wired configurable home tickers.
- Added `$TICKER` quote lookup to the caguastore search/omnibox, including price, daily move, market status, and previous close.
- Fixed quote symbol validation so Yahoo symbols containing `=` and `^` are accepted, enabling FX and index symbols.
- Updated `crates/fleet/src/serve/templates/store.html`, `crates/fleet/assets/store.js`, `crates/fleet/assets/store.css`, `crates/fleet/src/serve/routes.rs`, `crates/fleet/src/serve/mod.rs`, and `crates/fleet/src/config.rs`.
- Updated service-worker cache versions so the new frontend assets load.
- Added `[serve] tickers` configuration to `caguaserver:~/.config/fleet/fleet.toml` for BTC, RTX, DVN, USDMXN, and Brent.
- Ran `node --check assets/store.js`, `cargo build -p fleet`, `cargo test -p fleet`, local browser smoke tests, and Chrome/Firefox visual checks.
- Deployed with rsync and restarted `fleet-serve` and `minimonitor-agent` on caguaserver.
- Verified the live `/api/tickers` endpoint and the caguastore home page in Chrome.
- Marked caguastore task 1354 as done.
- Corrected the XLE sale record using the current quote assumption: 21.53 shares, approximately $1,260 value, and $6,228 cash.

## Decided
- Use Yahoo Finance's public quote endpoint because it covers stocks, ETFs, FX, futures, and crypto without a passkey.
- Keep the home widget public and outside the money-PIN gate because quote data is non-sensitive.
- Use the top three holdings plus peso and Brent as the initial watchlist.
- Keep the existing mercado page unchanged because it depends on the unavailable portfolio service on port 3010.
- Make the watchlist configuration-driven so holdings can be reordered without rebuilding.

## Next
- On the phone, hard-refresh caguastore or clear its service-worker cache, then verify the home widget and `$AMD` lookup.
- If the watchlist changes, edit `[serve] tickers` in `~/.config/fleet/fleet.toml` and run `sudo systemctl restart fleet-serve`.
- Investigate caguaphone SSH connectivity if phone-side diagnostics are needed; the observed SSH attempt timed out.
- Decide separately whether to replace or repair the legacy PIN-gated mercado screen and its missing portfolio service.

## Wiki?
- [[wiki/_inbox/allow-special-yahoo-symbols-in-quote-proxies|Quote Symbol Validation]] (auto-drafted)

## Signals
next_action: "Hard-refresh caguastore on the phone and verify that the home tickers widget and `$AMD` quote lookup render."
