---
date: 2026-08-04
project: minimonitor
type: session-note
week: 2026-W32
billable: —
tags: [project/minimonitor, session-note, week/2026-W32, auto-scribed]
---
# minimonitor — 2026-08-04

## State
Caguastore's omnibox is deployed and its PWA cache was invalidated; slash commands, direct URLs, Hermes fallback, and the Hermes Hub continuation button are live. The service inventory remains unchanged except that the Crux tile was clarified as the personal climbing trainer; restart cold by opening the minimonitor repo, checking git status and recent commits, then using the existing rsync, release-build, systemd restart, and curl verification flow.

## Did
- Implemented caguastore omnibox behavior in crates/fleet/assets/store.js: /g, /x, and /m shortcuts, Google-only search fallback, direct opening for domain-like input, and Hermes fallback for unknown text.
- Added desktop keyboard focus behavior and a slash-command hint row in crates/fleet/src/serve/templates/store.html and crates/fleet/assets/store.css.
- Added a 'continuar en hermes' button linking the current quick conversation into Hermes Hub.
- Validated JavaScript with node --check and ran cargo test -p fleet successfully.
- Verified Google search, direct URL opening, Hermes fallback, and the continuation button in a local browser session.
- Deployed to caguaserver by rsync, release-building fleet, swapping the binary, and restarting fleet-serve.
- Fixed stale PWA assets by bumping store.js asset versioning and the service-worker cache version, then redeployed and confirmed the live site.
- Kept polybot, crag-finder, depas, poker helper, and the main Crux trainer; clarified the Crux tile as 'entrenador de escalada' rather than removing it.
- Committed and pushed the caguastore and tile updates to main.

## Decided
- Do not shelve the reviewed services: crag-finder is a separate research project, paros remains a startup idea, polybot continues, depas is ongoing, poker helper remains a demo, and Crux is an actively used personal trainer.
- Remove only the misleading playground implication from Crux's presentation because there is no separate playground service.
- Do not automate betting on real-money poker sites or evade detection; pursue a self-hosted poker simulation instead.

## Next
- No immediate blocker remains; the latest deployment is live.
- If extending the poker helper, build an offline/self-hosted simulator with scripted opponents, automated hands, strategy metrics, and replay or range visualizations.
- Keep the off-site backup and modern-server migration as infrastructure priorities before adding photos, NAS, or Immich.

## Wiki?
- [[wiki/_inbox/pwa-service-worker-asset-cache-busting|PWA Cache Busting]] (auto-drafted)

## Signals
next_action: "Build or schedule the encrypted off-site backup before expanding the family photos and NAS stack."
