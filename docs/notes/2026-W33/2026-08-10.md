---
date: 2026-08-10
project: minimonitor
type: session-note
week: 2026-W33
billable: —
tags: [project/minimonitor, session-note, week/2026-W33, auto-scribed]
---
# minimonitor — 2026-08-10

## State
The demo is live at https://demo.javierr.com on isolated Hetzner server `bdlg-demo` (178.156.186.82), with PIN login and the planted-agent test verified end to end. The caguastore tile is deployed and the revival task is marked done; only owner-side follow-ups remain. To restart cold, load the Hetzner token from `~/Desktop/1/tools/vps/.hcloud-token`, use SSH key `~/.ssh/id_ed25519_claude`, and consult `~/.claude/projects/-Users-caguabot/memory/lawyers-demo-agent.md` for the redeploy topology.

## Did
- Installed the `hcloud` CLI and provisioned Hetzner server `bdlg-demo` as a cpx11 in Ashburn with Ubuntu 24.04.
- Hardened the VM with UFW allowing only 22/80/443, fail2ban, unattended upgrades, and an unprivileged `demo` runtime user.
- Synced hermes-agent, the sealed `bdlgdemo` profile, and `bufetedelagarza/ia-onprem/demos/web-chat/` to the VM.
- Configured the hermes read jail for the VM and ran the demo web chat as systemd unit `bdlg-demo-web` behind Caddy with a Let's Encrypt certificate.
- Deleted the dead Cloudflare `bdlg-demo` Worker and custom domain, replacing them with a proxied A record to the Hetzner VM.
- Verified the public landing page, PIN login, and an agent response containing the planted expediente AC-2026-041 demo data.
- Updated `lawyers-demo-agent.md` and `MEMORY.md` with the new topology and redeploy recipe.
- Added the `abogados·demo` caguastore tile in `crates/fleet/src/store.rs`, ran `cargo test -p fleet`, rebuilt and deployed minimonitor to caguaserver, and committed/pushed the change with message `store: abogados-demo tile (demo.javierr.com on Hetzner VM)`.
- Created and marked done board task 1457 for the Hetzner revival.

## Decided
- Keep the demo on an isolated Hetzner VM rather than exposing the mini through Tailscale Funnel.
- Keep the demo VM off the tailnet with zero fleet credentials; only the demo profile's own OpenRouter key is present.
- Use a direct Cloudflare-proxied DNS record instead of a Worker proxy because the Worker added an unnecessary failure point.
- Use the existing `bot` icon for the caguastore tile because it already exists and required no template changes.

## Next
- Rotate the Hetzner API token because it was pasted into the session transcript, then replace `~/Desktop/1/tools/vps/.hcloud-token` with the new token and revoke the old one.
- Owner-side task 1413 remains: produce the 90-second vertical video and email Artemio; the demo link is now unblocked.
- Provide a Meta test number or spare eSIM if the demo WhatsApp lane is still required.
- Decide whether visitor logs at `/home/demo/.hermes/profiles/bdlgdemo/webchat-logs/` should be added to the weekly backup rotation.

## Wiki?
- [[wiki/_inbox/rotate-pasted-infrastructure-tokens|Rotate Pasted Infrastructure Tokens]] (auto-drafted)

## Signals
next_action: "Rotate and revoke the exposed Hetzner API token, then update the local token file."
