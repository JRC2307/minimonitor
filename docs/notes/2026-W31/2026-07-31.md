---
date: 2026-07-31
project: minimonitor
type: session-note
week: 2026-W31
billable: —
tags: [project/minimonitor, session-note, week/2026-W31, auto-scribed]
---
# minimonitor — 2026-07-31

## State
Hermes agent is upgraded to v0.19.1 and running on the mini as the central hub. The official web UI is accessible via `hermes dashboard` behind tailscale at `http://100.105.239.50:9119` (auth cagua/caguamini), and an Electron desktop app is installed on the Air as a satellite that can connect to the mini's backend. The minimonitor caguastore has two new tiles: `hermes·tui` (ttyd at :7683) and `hermes·app` (dashboard at :9119). To restart cold: ensure LaunchAgents `com.caguabot.ttyd-hermes`, `com.caguabot.ttyd-hermes-proxy`, and `ai.hermes.serve` are loaded on the mini, then verify `curl http://100.105.239.50:9119/api/status` and `curl https://js-mac-mini.tail82f3c6.ts.net:7683/` both return 200 with auth.

## Did
- Upgraded hermes-agent on the mini from v0.2.1 to v0.19.1, cherry-picking 4 local commits onto upstream main and migrating configs v8→v33
- Set up ttyd web terminal on the mini at :7683 via LaunchAgents `com.caguabot.ttyd-hermes` and `com.caguabot.ttyd-hermes-proxy` with basic-auth
- Exposed ttyd terminal behind tailscale serve HTTPS at `https://js-mac-mini.tail82f3c6.ts.net:7683`
- Added `hermes-tui` StoreApp tile to minimonitor `crates/fleet/src/store.rs` and committed, deployed to caguaserver
- Installed hermes-agent v0.19.1 on the Air via official installer
- Authored fleet-aligned SOUL.md for the Air (satellite doctrine: no coding, fleet-brief orientation, state on caguaserver, notifications via `ssh caguamini wa-notify`)
- Configured Air's hermes config.yaml with custom provider pointing at mini's CLIProxyAPI (`gpt-5.6-sol`) over tailnet
- Added `OPENROUTER_API_KEY` fallback to Air's `~/.hermes/.env`
- Installed and built Hermes Electron desktop app on both the mini and the Air
- Switched mini's backend service from `hermes serve` to `hermes dashboard` via LaunchAgent `ai.hermes.serve.plist` on :9119 with basic-auth
- Added fleet-hub doctrine section to the mini's `~/.hermes/SOUL.md`
- Added `hermes-app` StoreApp tile to minimonitor and deployed to caguaserver (296 tests passing)
- Updated memory files `webterm-ttyd.md` and `hermes-on-mini.md`

## Decided
- Used mini's CLIProxyAPI (Codex subscription lane, `gpt-5.6-sol`) as Air's primary provider instead of OpenRouter or Anthropic to avoid new spend and preserve shared pool
- Converted backend from `hermes serve` to `hermes dashboard` to serve both web UI and desktop-app connection on same port
- Gave the mini its own fleet-hub SOUL.md and the Air a satellite SOUL.md rather than one shared file, because Hermes reads `~/.hermes/SOUL.md` per-machine and never reads CLAUDE.md
- Kept Air's local agent as degraded offline mode for when mini is unreachable, rather than pure thin client
- Integrated WhatsApp into upstream's plugin platform instead of custom adapter file, redesigned per-channel model selection to use upstream's per-session `/model` override system

## Next
- Manual step on the Air: in Hermes desktop app Settings → Gateway → Remote, enter URL `http://100.105.239.50:9119`, sign in with cagua/caguamini
- Set a stable signing secret on the backend if not done already so Air's keychain tokens persist across restarts
- Verify browser dashboard on phone or other tailnet device at `http://100.105.239.50:9119`

## Wiki?
- [[wiki/_inbox/hermes-soul-md-vs-claude-md|Hermes SOUL.md vs Claude Code CLAUDE.md]] (auto-drafted)

## Signals
next_action: "On the Air's Hermes desktop app, go to Settings → Gateway → Remote gateway, point it at `http://100.105.239.50:9119`, and sign in with cagua/caguamini to connect it to the mini's shared agent backend."

---

## Session 2 (auto-scribed)

## State
All work is shipped and live. The vitals dashboard on caguaserver:3016 has minute-level uPlot charts, an LLM ask box (via CLIProxyAPI), and the caguastore tile is deployed. Air whoop-push continuously syncs history. To restart cold: cd ~/Desktop/1/projects/personal/vitals, deploy server with rsync to caguaserver:services/whoop-snapshot/, restart whoop-push on caguaair via launchctl, and ensure LLM_API_KEY/LLM_BASE_URL env vars are set.

## Did
- Cleaned home folder: removed scratch scripts, polybot/, Playground/, .DS_Store
- Built minute-level HR history pipeline (task 1290): cursor-based idempotent sync in air/whoop-push, SQLite history.db on server, backfilled 246,605 samples since 2026-07-23
- Added endpoints: /api/hr-history, /api/day, /api/history/cursor, /api/ingest-history
- Rebuilt server/dashboard.html with vendored uPlot: day navigation, drag-zoom, night shading, HR-raise highlighting, SpO2 overlay, night/day summary cards, tappable event list
- Wired /api/ask LLM endpoint: packs day+previous-day summaries, calls CLIProxyAPI codex-sub lane (gpt-5.5) on the mini; tested English and Spanish
- Added vitals tile to minimonitor caguastore (task 1291): crates/fleet/src/store.rs, pulse icon, deployed fleet to caguaserver
- Updated CLAUDE.md (dashboard.html now canonical UI), committed both repos, closed board tasks 1290/1291/1309
- Verified phone-reactive UI at 390px in Chrome and Firefox; checked against real data (sleep 23:53→08:30, 8 HR-raise events)

## Decided
- Used CLIProxyAPI codex-sub lane (gpt-5.5) instead of real OpenAI key: no sk- key on fleet, zero cost, swappable via data/llm.env with no code change
- Vendored uPlot locally instead of CDN: required by caguaphone rules (no external deps)
- Cursor-based idempotent sync for history push: restart-safe, avoids re-pushing old data
- SQLite history.db on server for raw sample storage with minute-binned /api/day aggregation

## Next
- —

## Wiki?
- [[wiki/_inbox/ssh-cargo-not-on-path|SSH non-interactive shells: cargo not on PATH]] (auto-drafted)

## Signals
next_action: "No blockers — all tasks are done and deployed."
