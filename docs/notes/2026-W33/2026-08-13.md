---
date: 2026-08-13
project: minimonitor
type: session-note
week: 2026-W33
billable: —
tags: [project/minimonitor, session-note, week/2026-W33, auto-scribed]
---
# minimonitor — 2026-08-13

## State
The approved cleanup is complete: opencode was removed, Beszel was removed, Hermes dashboard was repaired, and Polybet's kill-date was repealed. RustDesk, caguainference, iprep-playground, poker-helper, and the other explicitly retained services remain untouched. Cold restart: inspect the mini's launchd services and caguaserver's observability compose, then verify Hermes at port 9119 and the fleet/store tile after any future hostname migration.

## Did
- Removed opencode services, including the unauthenticated ttyd on port 7682 and opencode web on port 4096; deleted ~/Library/LaunchAgents/com.caguabot.opencode.plist and com.caguabot.ttyd-opencode.plist and verified both ports closed.
- Fixed Hermes dashboard launchd configuration in ~/Library/LaunchAgents/ai.hermes.serve.plist by replacing stale Tailscale IP 100.105.239.50 with 100.124.66.77; bootstrapped the service and verified port 9119 returns its login redirect.
- Removed Beszel and beszel-agent containers from caguaserver's ~/services/observability/docker-compose.yml and verified uptime-kuma and ntfy were unaffected.
- Removed the Beszel tile from ~/Desktop/1/tools/minimonitor/crates/fleet/src/store.rs, ran fleet tests, committed the change, synchronized the repo to caguaserver, rebuilt fleet, and redeployed caguastore.
- Updated Polybet memory to repeal the scanner/panel kill-date and retain both services indefinitely.
- Recorded the RustDesk decision in memory: keep the server relay because it is still used for dial-in to the mini.
- Kept caguainference, iprep-playground, poker-helper, depas, paros, feishin, shoptpl, and backup files unchanged as requested.

## Decided
- Remove opencode because it exposed an unauthenticated writable shell on the tailnet and its experiment is no longer needed.
- Remove Beszel because it was not being used and consumed resources; leave uptime-kuma and ntfy in place.
- Keep RustDesk despite the web terminal because the RustDesk server relay is still needed to dial into the mini.
- Keep caguainference available for use when physically local to that machine.
- Keep Polybet scanner and panel running without the previously planned kill-date.
- Keep iprep-playground as a useful data source and poker-helper as a recreational tool.

## Next
- Open uptime-kuma and delete any stale monitor that still targets the removed Beszel service.
- Optionally run a final listener and process check for ports 7682 and 4096, and confirm the Beszel tile remains absent from caguastore.
- When convenient, complete the remaining phone-side app/PWA reinstalls and the cosmetic Tailscale renames; caguaphone remains unavailable for an on-device sweep while offline.

## Wiki?
- [[wiki/_inbox/stale-tailnet-ip-in-launchd-service|Stale Tailnet IPs Break Launchd Services]] (auto-drafted)

## Signals
next_action: "Delete any stale Beszel monitor in uptime-kuma, then verify the fleet/store page no longer references Beszel."

## Session 2

## State
caguastore live with the `tacto` tile (dev section, ext-style, no LED) pointing at
https://caguair.triceratops-adelie.ts.net:8810. Store rebuilt + redeployed on
caguaserver (fleet-serve active, tile verified in served HTML). The tacto tool
itself lives in its own repo (`~/Desktop/1/tools/tacto`, caguagit `tacto.git`):
Swift phone-as-trackpad running on the AIR as LaunchAgent `com.cagua.tacto`,
fronted by tailscale serve :8810 there. Cold restart of the store deploy loop:
rsync repo → `caguaserver:src/minimonitor/`, `~/.cargo/bin/cargo build --release
-p fleet`, stop/cp/start `fleet-serve`.

## Did
- Added `tacto` StoreApp to `crates/fleet/src/store.rs` (first as a mini-hosted
  tls-style entry :8810, then repointed to the Air as `ext` since the Air runs no
  minimonitor-agent → no LED to light). Reused the existing `hand` icon, hue 205.
- `cargo test -p fleet` green both times (298 tests); two deploys to caguaserver.
- (Own repo) Built tacto end to end: zero-dep Swift TCP listener serving the touch
  page + hand-rolled WebSocket upgrade on one port, gestures → CGEvents; embedded
  180px apple-touch-icon as data URI; exit-on-bind-failure so KeepAlive recovers;
  deployed to the Air, torn down on the mini.

## Decided
- tacto runs on the Air, not the mini (owner call mid-session): the trackpad is
  most useful on the portable machine.
- Tile is `ext`-style (no port/host): the Air has no agent, a dead LED is worse
  than none.
- Native iOS / Bluetooth versions rejected: iOS forbids apps the BT HID
  peripheral role; the PWA over tailnet is the only and better route.

## Next
- Owner: Accessibility grant for `~/.local/bin/tacto` on the Air (Settings pane
  left open there), then kickstart and confirm log says `accessibility: true`.
- If scroll feels inverted on real use: flip the `dy` sign in tacto `page.swift`.

## Wiki?
- launchd agents whose executable lives under ~/Desktop hang forever in dyld
  (TCC folder protection, zero log output) → generalizable, writing wiki page.

## Signals
next_action: "Run /code-review on fleet-phase-0-1, then merge to main via finishing-a-development-branch"
milestone: shipped
